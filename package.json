{
  "name": "@tinyauth/base",
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "biome": "biome"
  },
  "workspaces": [
    "./packages/*",
    "./examples/*"
  ],
  "author": "",
  "license": "AGPL-3.0-only",
  "description": "",
  "devDependencies": {
    "@biomejs/biome": "catalog:"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "libsql"
    ]
  }
}
