{
  "name": "@tinyrack/tinyauth-base",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "pnpm --filter '@tinyrack/tinyauth-*' --parallel dev",
    "test": "pnpm -r --workspace-concurrency=1 test && pnpm --filter @tinyrack/tinyauth-standalone test:dist",
    "build": "pnpm -r build",
    "biome": "biome",
    "tools": "tinyauth-tools",
    "test:setup:browsers": "pnpm --filter @tinyrack/tinyauth-frontend exec playwright install"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@tinyrack/tinyauth-tools": "workspace:*"
  }
}
