---
title: 테마
description: Tinyauth 프론트엔드의 테마 및 다크 모드 구성하기
section: configuration
order: 11
---

`tinyauth` 의 프론트엔드는 [daisyUI](https://daisyui.com/)를 기반으로 구축됐어요. 이 덕분에 다양한 테마 옵션을 제공하며, 선호하는 인터페이스 모양과 느낌을 지정할 수 있어요. 설정의 예시는 다음과 같아요.

```yaml
branding:
  theme_mode: system
  light_theme: light
  dark_theme: dark
```

- `theme_mode`: 테마 전환 모드를 설정해요.
  - `system`: 시스템 설정에 따라 라이트 또는 다크 모드를 자동으로 선택해요. 사용자에게 테마 전환 토글이 제공돼요.
  - `light`: 항상 라이트 모드의 테마를 사용해요.
  - `dark`: 항상 다크 모드의 테마를 사용해요.
- `light_theme`: 라이트 모드에서 사용할 테마 이름이에요. 기본값은 `light`예요.
- `dark_theme`: 다크 모드에서 사용할 테마 이름이에요. 기본값은 `dark`예요.

:::note
사용 가능한 테마의 종류와 적용 모습은 [daisyUI 테마 문서](https://daisyui.com/docs/themes/)에서 확인할 수 있어요. 현재 사용 가능한 테마 목록은 다음과 같아요.

`light`, `dark`, `cupcake`, `bumblebee`, `emerald`, `corporate`, `synthwave`, `retro`, `cyberpunk`, `valentine`, `halloween`, `garden`, `forest`, `aqua`, `lofi`, `pastel`, `fantasy`, `wireframe`, `black`, `luxury`, `dracula`, `cmyk`, `autumn`, `business`, `acid`, `lemonade`, `night`, `coffee`, `winter`, `dim`, `nord`, `sunset`, `caramellatte`, `abyss`,
:::

---

## 예시

만약 `light` 모드에서는 `cupcake` 테마를, `dark` 모드에서는 `dracula` 테마를 사용하고 싶다면 다음과 같이 설정할 수 있어요.

```yaml
branding:
  theme_mode: system
  light_theme: cupcake
  dark_theme: dracula
```

만약 `light` 모드로 고정하면서 `emerald` 테마를 사용하고 싶다면 다음과 같이 설정할 수 있어요.

```yaml
branding:
  theme_mode: light
  light_theme: emerald
```
