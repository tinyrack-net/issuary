---
title: 1분 시작
description: 1분 안에 Tinyauth를 실행해 보세요
---

`Tinyauth`를 3분 만에 실행해 보세요! 아래 단계에 따라 Docker 컨테이너로 빠르게 시작할 수 있어요.

```bash
docker run \
  --rm \
  -p 8080:8080 \
  -e COOKIE_SECRET="${openssl rand -hex 32}" \
  tinyrack/tinyauth
```