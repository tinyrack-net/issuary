{
  "name": "@issuary-server-examples/node-hono-sqlite",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @tinyrack/issuary-frontend build && cross-env NODE_OPTIONS=--conditions=@issuary/source tsx watch src/index.ts",
    "start": "pnpm --filter @tinyrack/issuary-frontend build && node --conditions=@issuary/source --import tsx src/index.ts",
    "build": "tsc --noEmit",
    "test": "pnpm --filter @tinyrack/issuary-server build && pnpm --filter @tinyrack/issuary-frontend build && pnpm run test:prepared",
    "test:prepared": "node --conditions=@issuary/source --import tsx src/smoke-test.ts"
  },
  "dependencies": {
    "@hono/node-server": "catalog:",
    "@tinyrack/issuary-server": "workspace:*",
    "hono": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "cross-env": "catalog:"
  }
}
